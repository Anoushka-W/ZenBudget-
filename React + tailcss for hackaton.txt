React + tailcss for hackaton

// import logo from './logo.svg';
// import './App.css';
import React, { useState } from "react";

// Example transactions data
const transactions = [
  { date: "2025-04-20", description: "Dinner at restaurant", amount: "$30", category: "Dining", mood: "😄" },
  { date: "2025-04-19", description: "Groceries", amount: "$60", category: "Groceries", mood: "😐" },
  { date: "2025-04-18", description: "Uber ride", amount: "$20", category: "Transport", mood: "😟" },
];

// Transaction List Component
function TransactionList({ transactions }) {
  return (
    <div className="overflow-x-auto mb-6">
      <table className="min-w-full bg-white rounded-lg shadow-md">
        <thead>
          <tr className="text-left text-gray-700">
            <th className="p-4">Date</th>
            <th className="p-4">Description</th>
            <th className="p-4">Amount</th>
            <th className="p-4">Category</th>
            <th className="p-4">Mood</th>
          </tr>
        </thead>
        <tbody>
          {transactions.map((transaction, index) => (
            <tr key={index} className="border-t">
              <td className="p-4">{transaction.date}</td>
              <td className="p-4">{transaction.description}</td>
              <td className="p-4">{transaction.amount}</td>
              <td className="p-4">{transaction.category}</td>
              <td className="p-4">{transaction.mood}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}

// Mood & Context Input Component
function MoodContextInput() {
  const [mood, setMood] = useState("😄");
  const [context, setContext] = useState("");

  return (
    <div className="my-6">
      <div className="flex justify-between mb-4">
        <label className="text-lg">Mood:</label>
        <select
          className="p-2 border rounded-md"
          value={mood}
          onChange={(e) => setMood(e.target.value)}
        >
          <option value="😄">Happy 😄</option>
          <option value="😐">Neutral 😐</option>
          <option value="😟">Sad 😟</option>
        </select>
      </div>

      <div className="flex justify-between mb-4">
        <label className="text-lg">Context:</label>
        <input
          type="text"
          className="p-2 border rounded-md"
          placeholder="e.g. at home, at work"
          value={context}
          onChange={(e) => setContext(e.target.value)}
        />
      </div>
    </div>
  );
}

// Agent Suggestion Box Component
function AgentSuggestion() {
  return (
    <div className="bg-blue-200 p-4 rounded-lg shadow-md">
      <h2 className="text-xl font-semibold">Agent's Suggestion</h2>
      <p className="mt-2">Cut back on dining this week to meet savings goal.</p>
      <p className="text-sm text-gray-600 mt-1">"Because your entertainment spending is 40% higher than usual."</p>
    </div>
  );
}

// Main App Component
function App() {
  return (
    <div className="min-h-screen bg-gray-50 flex flex-col items-center">
      <h1 className="text-4xl font-bold mt-8">AutoPilot Budgeting+</h1>

      <div className="w-full max-w-4xl mt-10 p-6 bg-white rounded-lg shadow-lg">
        <h2 className="text-2xl font-semibold mb-6">Transaction List</h2>
        <TransactionList transactions={transactions} />

        <MoodContextInput />

        <AgentSuggestion />
      </div>
    </div>
  );
}

export default App;















import React, { useState } from "react";

// Example transactions (these are just sample data)
const transactions = [
  { date: "2025-04-20", description: "Dinner at restaurant", amount: "$30", category: "Dining", mood: "😄" },
  { date: "2025-04-19", description: "Groceries", amount: "$60", category: "Groceries", mood: "😐" },
  { date: "2025-04-18", description: "Uber ride", amount: "$20", category: "Transport", mood: "😟" },
];

// Transaction List (this shows the list of transactions)
function TransactionList() {
  return (
    <div>
      <h2>Transaction List</h2>
      <table>
        <thead>
          <tr>
            <th>Date</th>
            <th>Description</th>
            <th>Amount</th>
            <th>Category</th>
            <th>Mood</th>
          </tr>
        </thead>
        <tbody>
          {transactions.map((transaction, index) => (
            <tr key={index}>
              <td>{transaction.date}</td>
              <td>{transaction.description}</td>
              <td>{transaction.amount}</td>
              <td>{transaction.category}</td>
              <td>{transaction.mood}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}

// Mood & Context Input (this is where users pick their mood and input context)
function MoodContextInput() {
  const [mood, setMood] = useState("😄"); // Default mood is happy
  const [context, setContext] = useState(""); // No context by default

  return (
    <div>
      <h3>Choose your mood</h3>
      <select value={mood} onChange={(e) => setMood(e.target.value)}>
        <option value="😄">Happy</option>
        <option value="😐">Neutral</option>
        <option value="😟">Sad</option>
      </select>

      <h3>Where are you?</h3>
      <input 
        type="text" 
        placeholder="e.g. at home, at work" 
        value={context} 
        onChange={(e) => setContext(e.target.value)} 
      />
    </div>
  );
}

// Agent Suggestion (this is where the AI suggests things based on the data)
function AgentSuggestion() {
  return (
    <div>
      <h3>Agent's Suggestion</h3>
      <p>Cut back on dining this week to meet savings goal.</p>
      <p>Because your entertainment spending is 40% higher than usual.</p>
    </div>
  );
}

// Main App (the main part that combines everything)
function App() {
  return (
    <div>
      <h1>AutoPilot Budgeting+</h1>

      <TransactionList />
      <MoodContextInput />
      <AgentSuggestion />
    </div>
  );
}

export default App;
